<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <PreferenceCategory android:title="@string/pref_header_general">

        <ListPreference
            android:defaultValue="1"
            android:entries="@array/location_method_list_titles"
            android:entryValues="@array/location_method_list_values"
            android:key="location_method"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"
            android:title="@string/pref_locatoin_method"
            />

        <CheckBoxPreference
            android:defaultValue="false"
            android:key="with_sound"
            android:summary="@string/pref_description_with_sound"
            android:title="@string/pref_title_with_sound"
            />

        <ListPreference
            android:defaultValue="1000"
            android:key="fire_distance"
            android:summary="@string/pref_fire_distance_description"
            android:title="@string/pref_fire_distance"
            android:entries="@array/pref_distance_titles"
            android:entryValues="@array/pref_distance_values"
            />
        <!-- NOTE: EditTextPreference accepts EditText attributes. -->
        <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
        <!--<EditTextPreference-->
            <!--android:capitalize="words"-->
            <!--android:defaultValue="@string/pref_default_display_name"-->
            <!--android:inputType="textCapWords"-->
            <!--android:key="example_text"-->
            <!--android:maxLines="1"-->
            <!--android:selectAllOnFocus="true"-->
            <!--android:singleLine="true"-->
            <!--android:title="@string/pref_title_display_name"/>-->

        <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
             dismiss it. -->
        <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
        <!--<ListPreference-->
            <!--android:defaultValue="-1"-->
            <!--android:entries="@array/pref_example_list_titles"-->
            <!--android:entryValues="@array/pref_example_list_values"-->
            <!--android:key="example_list"-->
            <!--android:negativeButtonText="@null"-->
            <!--android:positiveButtonText="@null"-->
            <!--android:title="@string/pref_title_add_friends_to_messages"/>-->
    </PreferenceCategory>

</PreferenceScreen>
